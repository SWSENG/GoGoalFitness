package com.example.gogoalfitness

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle
import com.example.gogoalfitness.databinding.ActivityUpperBodyBinding

class UpperBodyActivity : AppCompatActivity() {

    private lateinit var binding :ActivityUpperBodyBinding

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_upper_body)

        val imageId = intArrayOf(

        )
    }
}